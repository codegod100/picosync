{
  "name": "sqlite-sync-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "mkdir -p dist && esbuild src/index.ts --bundle --format=esm --outfile=dist/index.js --external:cloudflare:*",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "capnweb": "^0.1.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241022.0",
    "esbuild": "^0.23.1",
    "typescript": "^5.9.3",
    "wrangler": "^3.78.12"
  }
}