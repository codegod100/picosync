# SQLite Daemon Configuration

# For local Node.js daemon (default)
# VITE_SQLITE_DAEMON_URL=ws://localhost:8787/rpc

# For Cloudflare Worker (replace with your actual worker URL)
# VITE_SQLITE_DAEMON_URL=wss://your-worker.your-subdomain.workers.dev/rpc